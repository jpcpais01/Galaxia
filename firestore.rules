rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // No Firebase Auth — username is identity, session stored in localStorage
    match /{document=**} {
      allow read: if true;
      allow write: if true;
    }
  }
}
